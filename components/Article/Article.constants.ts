import { TAction, TIcon } from 'types';
import { ACTION_TYPES, ICON_TYPES } from 'variables';

export const ARTICLE_ACTIONS: TAction[] = [ACTION_TYPES.delete, ACTION_TYPES.markAsRead];

export const ARTICLE_ICONS: TIcon[] = [ICON_TYPES.save];
